{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd .. && npm install && npm run build && cp -r ../.next ./.next 2>/dev/null || true",
  "installCommand": "npm install"
}
