{
  "name": "techsol-media-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix client",
    "build": "cd client && npm install && npm run build",
    "start": "npm run start --prefix client"
  }
}
